import React, { useEffect, useReducer } from "react"
/*
Component will filter via dropdown list. Categories: Full periodic table, transition metals, metalloids, nonmetals, noble gases, radioactive, alkali metal, halogen, lanthanide, actinide
*/
const FilterBar = (props) => {
  return <>JSX</>
}

export default FilterBar
